//Function Expression Syntax
let getGreeting = function(name) {
  return 'Hello ' + name + '!';
}
console.log(getGreeting('Unnati')); 


//Arrow Function Syntax
getGreeting = (name) => 'Hello ' + name + '!';
    console.log(getGreeting('Veronica'));