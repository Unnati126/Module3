let three = "3";
let four = "4";
let thirty = "30";


console.log(addition = Number(three) + Number(four)); //three & four both are strings so, covert strings into numbers
console.log(multiplication = three * four);   //already correct
console.log(division = three / four);         //already correct
console.log(subtraction = three - four);      //already correct

console.log(lessThan1 = three < four); //already correct
console.log(lessThan2 = Number(thirty) < Number(four)); //thirty is a string so, convert it into number